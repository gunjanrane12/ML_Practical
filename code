import requests as rq

if not asq:
    asq=input()


if asg.isdigit():
    asg = int(asg)
    if asg < 6:
        link = f"https://tinyurl.com/irpra/IR_{asg}.ipynb"
        filename = f"Untitled_a.ipynb"
    else:
        link = f"https://tinyurl.com/bifpra/BIF_{asg-5}.ipynb"
        filename = f"Untitled_a.ipynb"
else:
    link = f"https://tinyurl.com/bifpra/{asg}"
    filename = asg

a = rq.get(link)
with open(filename, "wb") as f:
    f.write(a.content)
